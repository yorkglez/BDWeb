import { ScrollNavGuestDirective } from './scroll-nav-guest.directive';

describe('ScrollNavGuestDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollNavGuestDirective();
    expect(directive).toBeTruthy();
  });
});
