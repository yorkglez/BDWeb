import { ScrollGuestDirective } from './scroll-guest.directive';

describe('ScrollGuestDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollGuestDirective();
    expect(directive).toBeTruthy();
  });
});
